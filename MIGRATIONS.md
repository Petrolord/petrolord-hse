# Migration log (petrolord-hse)

Shared Supabase project: `ssyckywijlrkgcwvkwlr` (shared with Petrolord Suite).
Apply staging-first per Petrolord_Database_Conventions. Migrations touching
shared tables (organizations, users, invitations, onboarding) need a second
engineer's review.

| Migration | Applied | Notes |
|---|---|---|
| 20260720130000_hse_ai_usage.sql | 2026-07-20 (management-API query, live) | New `hse_ai_usage` table (per-org monthly AI metering, RLS read via `is_org_member`) + `hse_check_and_increment_ai_usage()` (SECURITY DEFINER, service_role-only execute). Quotas: free 30/month, paid 500/month. Verified: increment, deny-at-quota, grants. |
| 20260810200000_qr_security_lockdown.sql | 2026-08-10 (management-API query, live) | QR lockdown (launch Phase 1, PR #2): revoked writes on `public_qr_sites` view, added `resolve_qr_token` RPC (anon execute), replaced permissive `quick_reports` insert policy with org-scoped policy. View SELECT revoke deferred (see 20260810223000). |
| 20260810210000_site_type_rig_depot.sql | 2026-08-10 (management-API query, live) | Expanded `organization_sites_site_type_check` to allow `rig` and `depot` (launch Phase 2, PR #3). |
| 20260810220000_complete_org_setup_rpc.sql | 2026-08-10 (management-API query, live) | `hse_complete_org_setup` RPC for the launch checklist (organizations RLS is super-admin-only, so org admins need a SECURITY DEFINER path to set `setup_completed`) (launch Phase 3, PR #4). |
| 20260810223000_qr_view_select_revoke.sql | 2026-08-10 (management-API query, live) | Revoked SELECT on `public_qr_sites` from anon/authenticated, closing token enumeration. Applied only after the new SPA build (RPC-based QR resolution) was confirmed live on hse.petrolord.com. Verified: anon view SELECT 42501, bogus token → empty, real token resolves via RPC. |
| 20260919120000_hs1_safety_statistics.sql | **APPLIED 2026-09-21** (owner-run hs1-apply.sh, 8/8 checks ok after a rolled-back dry run with the same checks all ok) | HS1 Safety Statistics. New `public.hse_exposure_hours` (org, site nullable -> organization_sites ON DELETE RESTRICT, one calendar month per row, hours > 0, headcount, workforce employee/contractor/combined, source, notes, created_by; unique per org/site/month/workforce; RLS: members read, active supervisor-or-above members write, site must belong to the org, anon revoked). New `public.hse_is_stats_editor(org)` (SECURITY DEFINER, reads organization_members only). Additive NULLABLE columns on `public.quick_reports` (the live incident record): injury_classification, days_away, days_restricted, pse_classification, workforce, occurred_on, classified_by, classified_at, with 5 check constraints, plus trigger `hse_quick_reports_classification_guard` (only an editor may set or change the classification; the stamp is set by the database). No shared table altered. Backfill-free: existing reports stay NULL = unclassified. Rehearsed on scratch PG16 (`tools/hs1/scratch/run.sh`, applied twice + probes). **Owner, from this repo root with the project linked: `tools/hs1/hs1-apply.sh dry-run` (live data, one rolled-back transaction, every check row ok = true), then `tools/hs1/hs1-apply.sh apply`, then fill in this row.** Apply before uploading the HS1 bundle. |
| 20260919130000_hs2_occupational_hygiene.sql | **APPLIED 2026-09-21** (owner-run hs2-apply.sh, 19/19 checks ok after a rolled-back dry run with the same checks all ok) | HS2 Occupational Hygiene. New `public.hse_noise_samples` (periods jsonb [{levelDbA, durationH}] at most 24 h, criterion OSHA_PEL/OSHA_ACTION_LEVEL/NIOSH_REL/EU_LEX, shift hours, hearing protector method/NRR/weighting/C-weighted level/type with a completeness check), `public.hse_chemical_samples` (one row per agent: units, TWA and STEL limits as inputs with a required limit_source, periods jsonb [{concentration, durationH}], stel_periods jsonb [{concentration, durationMin}] at most 15 min, shift and weekly hours, mixture_group_id) and `public.hse_heat_assessments` (wbgt_form indoor/outdoor/measured with matching wbgt_periods jsonb, metabolic_periods jsonb, each at most 60 min, acclimatized). All org-scoped, site nullable -> organization_sites ON DELETE RESTRICT, optional worker_user_id who must belong to the org. Helpers: `hse_is_hygiene_editor(org)` (SECURITY DEFINER; active owner/admin/org_admin/super_admin/manager/supervisor/health_officer, or platform super admin; independent of HS1's unapplied `hse_is_stats_editor`), `hse_hygiene_worker_in_org(org, user)` (SECURITY DEFINER), `hse_hygiene_periods_ok(...)` (jsonb shape check), `hse_hygiene_stamp()` trigger (keeps created_by/created_at, stamps updated_by/updated_at, refuses an organization move). RLS: members read; editors insert/update/delete; forged created_by refused; anon revoked. No shared table altered. Rehearsed on scratch PG16 (`tools/hs2/scratch/run.sh`: applied twice, 19 checks ok, probes match). **Live dry run 2026-09-19 (`tools/hs2/hs2-apply.sh dry-run`, rolled back): 19/19 checks ok = true; afterwards 0 HS2 tables and 0 HS2 functions present, so nothing was kept.** Owner, from this repo root with the project linked: `tools/hs2/hs2-apply.sh dry-run`, then `tools/hs2/hs2-apply.sh apply`, then fill in this row. Order-independent of HS1. Apply before uploading the HS2 bundle (until then the module's calculators work and saving shows a "not switched on yet" notice). |
| 20260919150000_hse_public_registers.sql | **APPLIED 2026-09-21** (owner-run tools/hse-registers/apply.sh, 9/9 checks ok after a rolled-back dry run with the same checks all ok) | Fixes a live defect: Training & Competency, Contractor Safety and Safety Audits (plus the Security and dashboard tiles that read them) called `supabase.schema('hse')`, which PostgREST does not expose (PGRST106, confirmed 2026-09-19) and on which `authenticated` has no table privilege, so every list was empty and every create failed in production; every hse table they target holds 0 rows. Creates 11 new `public.hse_*` tables mirroring the hse originals column for column (`hse_training_programs`, `hse_training_schedule`, `hse_training_records`, `hse_competency_framework`, `hse_competency_assessments`, `hse_competency_records`, `hse_contractors`, `hse_safety_inductions`, `hse_contractor_incidents`, `hse_audit_schedule`, `hse_audit_findings`), org_id -> organizations ON DELETE CASCADE, site columns -> organization_sites, real parent FKs. RLS on all 11: read = active org member (`is_org_member`) or super admin; insert/update = active org member, created_by must be the caller, every referenced site/contractor/program/competency/audit must be in the same org; no delete policy or grant; anon revoked. No views. Does not touch the hse schema or any shared table. Rehearsed on scratch PG16 (`tools/hse-registers/scratch/run.sh`: applied twice, 9 checks, 17 probe groups). Rollback-wrapped dry run against live data 2026-09-19: all 9 checks ok = true, nothing kept. **Owner, from this repo root with the project linked: `tools/hse-registers/apply.sh dry-run`, then `tools/hse-registers/apply.sh apply`, then fill in this row. Apply BEFORE uploading the front end that reads these tables.** |
| 20260923120000_hse_internal_org_paid_ai_quota.sql | **APPLIED 2026-09-23** (owner-run `tools/hse-internal/apply.sh`: dry run 2/2 probe rows ok (internal org paid/500, free org free/30, rolled back), then apply with 6/6 read-only checks ok) | HSE Professional for internal organizations. Redefines only the body of `hse_check_and_increment_ai_usage` (same signature, SECURITY DEFINER, grants restated: service_role only) with one more OR branch: an organization with `organizations.is_internal` true (Lordsway Energy) counts as paid, 500 AI uses a month in place of 30. The shared `organizations` table is only read; no table, column, policy or grant changes. Pairs with the front end (src/lib/hseAccess.js: internal org = premium; Suite roles such as engineer read as staff). Live body confirmed identical to 20260720130000 first. **Rollback-wrapped dry run against live data 2026-09-23: probe internal org -> tier paid, quota 500; a free non-internal org -> tier free, quota 30; afterwards the function hash was unchanged (9322aace...) and 0 probe rows remained.** Read-only checks before applying: 5 of 6 ok, the expected false being 'function counts internal orgs as paid'. Order-independent of the upload. |
